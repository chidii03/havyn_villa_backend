/**
 * Booking capacity/availability/overlap validation, Redis hold, transactional confirm
 * with a Postgres exclusion constraint, status machine, cancellation/refund state,
 * domain events. Implemented in prompt 12. Never trusts frontend totals.
 */
package com.havyn.booking;
